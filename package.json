{
  "name": "novel-post-helper",
  "version": "0.5.0",
  "private": true,
  "description": "投稿画面への貼り込み係（Chrome拡張・Manifest V3）。ビルド工程は無く、このフォルダーをそのまま読み込んで動く。npmはテストのためだけに使う。",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}

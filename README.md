# 貼り込み係（novel-post-helper）

VS Code の拡張機能「統合小説執筆環境」（novel-ai-assistant）の相棒となる Chrome 拡張（Manifest V3）です。
統合小説執筆環境でコピーした原稿を、**作者が自分で開いた投稿画面の欄へ入れる**道具です。
0.2.0 から、**自分の管理画面に出ている読者の反応（PV・フォロワー・星・応援…）をコピーして
統合小説執筆環境へ戻す**こともできます。0.6.0 から、なろうの作品は**ご自分の作品の Narou.fun のページ**
（なろうの分析サイト `db.narou.fun`）からも読めます。

> ## ⚠ いまの状態：テスト中
>
> - **カクヨムの投稿画面の欄は、2026-09-22 に実機で確かめました**（`form#episode-editForm`、
>   `#episodeTitle-input`、`#episodeBody-input`）。本文欄は `textarea` なので、改行はそのまま入ります
> - **カクヨムの読者の反応の読み取りは、2026-09-22 に実機で押しました。** 作品管理の画面から
>   コピーでき、統合小説執筆環境の台帳へ3件（★1,612／今日のPV 1／今月のPV 667）入りました。ただし
>   このとき**「読者からの反応」6欄のうち★しか取れなかった**ので、0.2.1 で正確な数
>   （数字にマウスを当てたときに出る数）から読むよう直しました。**この直しを押し直しての確認は、まだです**
> - **話ごとの反応は、作品管理の画面から1回で全話ぶん読めます**（0.4.0から）。
>   **この画面での話ごとの読み取りは、まだ実機で押していません**
> - **各話の更新日と、直近の日ごとのPVも作品管理の画面から読みます**（0.5.0から。統合小説執筆環境の離脱率と
>   日ごとのグラフの材料です）。**こちらもまだ実機で押していません**
> - **アクセス数の画面は50話ずつのページ送りです**（219話の作品で5ページ）。1回のコピーで入るのは
>   その画面に出ている50話ぶんだけなので、次のページがあるときは「このページの分だけです」と出ます（0.2.2）。
>   ページごとにコピー→取り込みを繰り返してください（統合小説執筆環境は追記するので、二重にはなりません）。
>   **作品管理の道ができたあとも、この道は残してあります**（片方の画面の作りが変わった日の逃げ道）
> - **アルファポリスは、投稿も読み取りも実機未確認です。** 欄の指定は推測のままで、
>   読み取りは表に枠だけ置いて止めてあります
> - **Narou.fun の作品ページの読み取り（0.6.0）は、まだ実機で押していません。** ページの形は
>   2026-09-23 に開発の場で読んだ実物に合わせてあります（サーバーが組んだHTMLの札の並び）
> - **Narou.fun の日ごとのブクマと評価の表・最終取得日時（0.7.0）も、拡張を入れて押したことはまだありません。**
>   2026-09-23 に、ブラウザで組まれたあとの実物のページへ読み取り係をそのまま読み込ませて試し、
>   表示件数30で29日ぶん（減った2日を含む）が、ページの元の数と一致して読めました
>
> 欄や数字が見つからないときは「ページの形が変わったようです」と言って**何もしません**ので、
> 書きかけの原稿が壊れることも、読んでいない数字が統合小説執筆環境へ渡ることもありません。実機で確かめたら、
> [`content/sites.js`](content/sites.js)（貼り込み）と [`content/statsSites.js`](content/statsSites.js)（読み取り）の
> 表——**指定はこの2か所だけ**——を直してください。
>
> 確かめが要ることは、この README の末尾「実機で確かめること」に並べてあります。

---

## 越えない一線

統合小説執筆環境の「サイトへHTTPを1行も書かない」（設計書6.68）と同じ思想です。**約束ではなく、コードで守ります**
（`test/redLine.test.js` がソースを走査して、下の3つを毎回確かめます）。

1. **通信を1本もしません。** `fetch`・`XMLHttpRequest`・`WebSocket`・ビーコンのたぐいを1行も書いていません。
   動くのは、作者が自分で開いたページの中だけです
2. **投稿ボタンを絶対に押しません。** クリックや送信を起こすコードがありません。欄を埋めたところで終わりで、
   内容を確かめて投稿するのは作者です（なろうの規約が明確に禁じるのは、この「自動投稿」です）
3. **認証情報に触れません。** パスワード欄のあるページでは、欄を探す前に降ります（「ログイン画面では動きません」）。
   Cookie もセッションも読み書きしません。ログインは、いつもどおりブラウザで作者が行ってください
4. **読むのは、ご自分の管理画面と、ご自分の作品の Narou.fun のページに出ている「ラベルと数の組」だけです**
   （設計書6.79.7の枠）。貼り込みのときに見るのは「埋める欄が在るか」「その欄が空か」だけ。読み取りのときも、
   本文・他の方の作品・ページ全体の文字は読みません（読む文字には長さの上限を掛けてあります）。
   集めてよいのは [`content/read.js`](content/read.js) だけで、**読んだものの行き先はクリップボードだけ**です
   ——どこにも保存せず、どこへも送りません。対応するのはカクヨムと Narou.fun の作品ページだけで、
   **なろう本体（syosetu.com）とそのアクセス解析（KASASAGI）**・pixiv・ハーメルン・note では読み取りを
   行いません（アルファポリスは規約上は可ですが、画面を実機で確かめるまで止めてあります）。
   **Narou.fun は誰の作品のページでも開けるので、この拡張には作者の作品かどうかが分かりません。**
   取り込むときに、統合小説執筆環境が作品ID（Nコード）でご自分の作品かどうかを確かめ、ほかの方の作品の数は取り込みません
5. **開いただけでは動きません。** ポップアップのボタンを押したときに、1回だけ動きます。
   ページを見張る仕組み（MutationObserver・setInterval）は1行もありません

### 権限（できるだけ少なく）

| 権限 | 何のために |
|---|---|
| `clipboardRead` | 統合小説執筆環境がコピーしたデータを読むため |
| `clipboardWrite` | 読んだ読者の反応を、統合小説執筆環境へ渡すためにクリップボードへ置くため（0.2.0で追加） |
| `activeTab` | いま開いているタブのURLを見て、貼り込み先・読み取り先が合っているか確かめるため |
| `content_scripts` の `matches` | カクヨムの投稿まわり・管理画面と、アルファポリスの投稿まわりのページ、Narou.fun の作品ページ（`https://db.narou.fun/works/*`。0.6.0）にだけ入るため |

`host_permissions` は**書いていません**。ページへ入る範囲は `manifest.json` の `content_scripts.matches`
だけが決めており、そこに書いていないサイトでは、この拡張は存在しないのと同じです。

---

## 導入のしかた（開発者モードで読み込む）

ストアには出していません。手元のフォルダーをそのまま読み込みます（ビルドは要りません）。

1. Chrome で `chrome://extensions` を開く
2. 右上の **「デベロッパー モード」** をオンにする
3. **「パッケージ化されていない拡張機能を読み込む」** を押し、このフォルダー（`novel-post-helper`）を選ぶ
4. ツールバーに「貼り込み係」が出る（見当たらなければ、パズルのかけらの印から固定する）

**直したあとは、`chrome://extensions` の再読み込みボタンを押し、投稿ページも再読み込みしてください。**
ページ側のスクリプトは、ページを開いたときに入るためです。

---

## 使い方の流れ

ポップアップを開くと、**いま開いている画面に合わせて、2つのボタンの使える・使えないが決まります**（0.3.0から）。
使えないボタンは押せない見た目になり、**その理由と、どの画面でなら使えるか**がボタンの下に1行出ます
（ボタンは消えません——消すと「壊れた」のか「この画面では使えない」のかが分からなくなるためです）。
押したあとの確かめ（作品の取り違え・読める管理画面か・ログイン画面か）は、これまでどおり全部そのまま働きます。

### その1　原稿を投稿画面へ入れる

1. **統合小説執筆環境（VS Code）** で、投稿する話を開き、投稿キットの **「貼り込み係へ渡す形でコピー」** を実行する
2. **ブラウザ** で、その作品の**話の新規作成画面**を開く（ログインは普通に済ませておく）
3. ツールバーの **「貼り込み係」** を押し、**「この画面に貼り込む」** を押す
4. タイトルと本文が入る。**内容を自分の目で確かめて、投稿ボタンは自分で押す**

途中で止まったときは、理由がそのまま出ます。

| 出る文 | 意味 |
|---|---|
| クリップボードに貼り込み用のデータがありません | 統合小説執筆環境でのコピーがまだ／別のものをコピーした |
| この画面は◯◯の投稿画面ではないようです | サイトが違う（封筒はカクヨム宛て、開いているのは別サイト等） |
| この画面は作品「…」の投稿画面ではないようです | **別の作品**の投稿画面を開いている（複数タブのときに起きる） |
| ◯◯の話の作成画面ではないようです | 作品の管理画面など、欄のないページを開いている |
| ログイン画面では動きません | セッションが切れている。ログインし直してから |
| ページの形が変わったようです | サイトのHTMLが変わり、欄を見つけられない（`content/sites.js` を直す） |

貼り込む前に確認を出すのは、次の2つのときです。断れば何もしません。

- **欄にすでに文が入っている**とき（書きかけの前書きを黙って消さないため）
- **その欄が「見当を付けた欄」だった**とき。`#body` や `contenteditable` のような、
  どのページにもあり得る形で当たった場合は、投稿の欄でない恐れがあるので、空でも一度お尋ねします
  （サイト固有の形——`textarea[name="body"]` など——で当たり、かつ空のときだけ、黙って入れます）

どの欄へ入れたのかは、結果の表示に `本文欄（textarea#episode-body）` の形で出ます。
見覚えのない欄の名前が出たら、貼り込み先が違っている合図です。

### その2　読者の反応を統合小説執筆環境へ戻す（0.2.0から。いまはカクヨムと、なろうの作品の Narou.fun）

1. **ブラウザ** で、次のどちらかの画面を開く（ご自分の作品の画面です）。
   **作品管理のほうが、1回で全部入ります**（0.4.0から）

   | | 作品管理 | アクセス数 |
   |---|---|---|
   | URL | `kakuyomu.jp/my/works/{作品ID}` | `kakuyomu.jp/works/{作品ID}/accesses` |
   | 話の数 | **1回で全話** | **50話ずつ**・ページ送り（219話なら5ページ） |
   | 話ごとに読む欄 | 応援・応援コメント・PV | 応援・PV |
   | 作品全体の反応 | 読む（フォロワー・PV・★・レビュー・応援・応援コメント）＋今日と今月のPV | 読まない |
   | 各話の更新日 | 読む（「最終更新」の日時。0.5.0から） | 読まない |
   | 直近の日ごとのPV | 読む（反応のグラフの1本ずつ。0.5.0から） | 読まない |
   | 押す回数 | **1回** | ページの数だけ |

2. ツールバーの **「貼り込み係」** を押し、**「この画面の読者の反応をコピーする」** を押す
3. 「読者の反応 251件（作品全体 2・日ごと 30・話ごと 219）をコピーしました」のように出る（作品全体の2は、反応の6欄と今月のPV）
4. **統合小説執筆環境（VS Code）** の作品管理メニューで **「読者の反応を貼り付けて取り込む」** を実行する

読むのは**そのとき画面に出ている数だけ**です。月を切り替えたり、次のページへ進んだりは
しません（別の期間が欲しいときは、ご自分で画面を切り替えて、もう一度押してください）。

**アクセス数のページに「次へ」があるときは、そう出ます**（0.2.2から）。1〜3の手順を
ページごとに繰り返してください——統合小説執筆環境の取り込みは追記なので、**繰り返しても二重にはなりません**。

| 出る文 | 意味 |
|---|---|
| このページの分だけです。「次へ」で次のページを開き… | 話が50話を超えていて、次のページがある |
| 日ごとの表は、画面に出ている行の分だけです。表の下の「表示件数」を30に… | Narou.fun の日ごとの表が10行（または20行）のまま（0.7.0） |
| この画面は、読者の反応を読める管理画面ではありません | 上の2つの画面ではない（話の編集画面や公開ページなど） |
| このサイトの読者の反応には対応していません | カクヨム・Narou.fun 以外のタブを開いている（なろう本体・KASASAGI もここ） |
| アルファポリスの読者の反応の読み取りには、まだ対応していません | 管理画面の形を実機で確かめられていないため止めてある |
| ページの形が変わったようです | 数字が1つも見つからない（`content/statsSites.js` を直す） |
| ログイン画面では動きません | セッションが切れている |

読めなかった欄は、**0ではなく「欄ごと無し」**で渡します（実機の「応援コメント –」がこれです）。
0と書いてしまうと、統合小説執筆環境の記録に「読んでいない数字」が残り、あとから見分けられなくなるためです。

#### なろうの作品：Narou.fun の作品ページ（0.6.0から）

なろう本体（syosetu.com）は規約の判断で読みません。代わりに、なろうの分析サイト **Narou.fun** の
**ご自分の作品のページ**（`https://db.narou.fun/works/{Nコード}`）から、作品全体の数を読みます。
統合小説執筆環境の執筆量パネル「サイトの記録」の **「分析（Narou.fun）を開く」** で開けるページです。

1. **ブラウザ** で、ご自分の作品の Narou.fun のページを開く
2. ページの下の日ごとの表で **「表示件数」を30にする**（0.7.0。10のままだと9日ぶんしか入りません）
3. **「この画面の読者の反応をコピーする」** を押す（「読者の反応 30件（作品全体 1・日ごと 29）をコピーしました」）
4. **統合小説執筆環境** で **「読者の反応を貼り付けて取り込む」** を実行する

| Narou.fun の表示 | 統合小説執筆環境の欄 |
|---|---|
| 総合P | `points`（評価） |
| ブクマ | `bookmarks`（ブックマーク） |
| 感想数 | `comments`（コメント） |
| レビュー | `reviews`（レビュー） |
| 評価P | `narou_ratingPoints`（評価ポイント） |
| 評価者数 | `narou_raters`（評価者数） |
| 週間読者 | `narou_weeklyReaders`（週間読者） |

**読まないもの**：

- **平均評価**——評価P ÷ 評価者数（10点満点の点の平均）で、なろうのバックアップの「評価平均」（星の平均）と
  尺度が違います。同じ欄へ入れると数字が化けるので読みません（評価Pと評価者数から割れます）
- **評価頻度**——評価者数 ÷ ブクマ で、統合小説執筆環境の**評価率（評価した人数 ÷ 第1話のPV）とは別物**です
- 日間P・日間イン・ベスト（順位まわり）、全話数・最終更新・字数（読者の反応ではない）、あらすじ・題
- 「解析」の行き先（**KASASAGI**。なろうの運営会社のアクセス解析）——なろう本体と同じ扱いで、辿りません

**日ごとのブクマと評価の表（0.7.0から）**：表の数は**その日までの累計**なので、**前の日との差**を
その日の数にします（ブクマ→`bookmarks`、評価→`narou_ratingPoints`。表の「評価」は評価Pです）。

- **ブクマが外された日は、マイナスのまま**統合小説執筆環境へ渡します（そちらのグラフでは0の線より下に赤で出ます）
- **表の最初の日（前の日が無い日）は入れません**。差が取れないためです（翌日以降の取り込みで埋まります）
- 括弧の中の差（`(+1)`）は読みません。累計どうしから自分で取ります（最初の行の「(0)」はサイトが置いた値です）
- 日付に年が無いので、**押した日から年を補います**（1月に押した12月の行は前の年）。
  押した日より先の日付の行（リアルタイム表示の「明日」）は入れません
- 表は10行ずつです。**「次へ」でページごとに取り込むのではなく、「表示件数」を30にしてください**
  ——ページの境目の日の差が取れずに抜けるためです。途中までのときは、コピーのあとにそう出ます

**記録の日時は、ページの「最終取得日時」です（0.7.0から）**。Narou.fun の数は、Narou.fun がなろうから
取ってきた時点のものなので、押した時刻ではなくこちらを使います。読めなかったときは押した時刻にし、
そうと分かる印を封筒に付けます（統合小説執筆環境の記録のメモに出ます）。

**ほかの方の作品のページで押しても、統合小説執筆環境は取り込みません。** Narou.fun は誰の作品のページでも開けるので、
この拡張には作者の作品かどうかが分かりません。取り込むときに、統合小説執筆環境が作品ID（Nコード）で
ご自分の作品かどうかを確かめます（統合小説執筆環境にその作品のNコードを登録していないときは、登録するまで取り込みません）。

**率（離脱率・ブックマーク率・評価率）は、Narou.fun だけでは出ません。** 分母の「第1話のPV」が
Narou.fun に無いためです（KASASAGI にありますが、読みません）。

### クリップボードについて（正直に書きます）

Windows の「クリップボードの履歴」「デバイス間の同期」を有効にしていると、コピーした本文が
Microsoft のクラウドを経由することがあります。これは統合小説執筆環境の「投稿サイト用に変換してコピー」でも同じですが、
未公開のまま取りやめる本文も通りうる、ということは知っておいてください。

---

## 受け渡しの形（封筒）

統合小説執筆環境とこの拡張が、クリップボード越しにやりとりする JSON です（設計書6.79.3）。

```json
{
  "novelai-post": 1,
  "site": "kakuyomu",
  "workId": "16816927859000000000",
  "title": "第13話 星の落ちる夜",
  "body": "（変換済みの本文）"
}
```

- `novelai-post` は版。**1でなければ読みません**（知らない版を推測で読むと、欄の意味が変わっていたときに
  本文を取り違えて入れることになるため）
- `site` は `kakuyomu` / `narou` / `alphapolis`
- `workId` は任意。**あれば、投稿画面のURLの作品IDと突き合わせます**（複数作品の取り違え防止）。
  ただし**アルファポリスでは、いまは突き合わせません**——URL上の作品IDの形が実機で確かめられておらず、
  当てずっぽうの照合が「たまたま一致」すると、確かめた顔で別の作品へ貼り込むことになるためです。
  この場合は結果の先頭に【…ご自分で確かめてください】と出ます
- `title` は空でも構いません（空なら、タイトル欄は触りません）
- `body` は50万字まで。それを超えるものは、統合小説執筆環境の側の取り違え（作品まるごと等）とみなして断ります
- **知らない欄は無視して読みます。** 統合小説執筆環境が将来 `caption` などを足しても、古い貼り込み係が壊れません

### 読者の反応の封筒（向きが逆。拡張 → 統合小説執筆環境）

こちらは、この拡張が作って**クリップボードへ置く**JSONです（設計書6.79.7）。統合小説執筆環境の
`core/readerStatsEnvelope.ts` が受けます。

```json
{
  "novelai-stats": 1,
  "site": "kakuyomu",
  "workId": "16816927859000000000",
  "readAt": "2026-09-22T01:00:00.000Z",
  "entries": [
    { "scope": "work", "metrics": { "bookmarks": 23, "pv": 1269, "points": 18, "reviews": 6, "likes": 33 } },
    { "scope": "work", "period": "day", "periodKey": "2026-09-22", "metrics": { "pv": 0 } },
    { "scope": "work", "period": "month", "periodKey": "2026-09", "metrics": { "pv": 2 } },
    { "scope": "episode", "episode": 1, "metrics": { "likes": 4, "pv": 102 } }
  ]
}
```

- `readAt` は**読んだ時刻**です（取り込んだ時刻ではありません）。Narou.fun の封筒だけは、ページの
  **最終取得日時**です（0.7.0から。下の `readAtBasis`）
- `workId` はURLから読んだ作品IDです。統合小説執筆環境はこれを台帳と突き合わせ、**別の作品の画面を開いたまま
  押したとき**に取り込みを止めます
- カクヨムの表示を統合小説執筆環境の欄へ写す対応は、フォロワー→`bookmarks`、PV→`pv`、★の数→`points`、
  レビュー→`reviews`、応援→`likes`、応援コメント→`comments` です
- **「読者からの反応」の6欄（フォロワー・PV・★・レビュー・応援・コメント）は、数字にマウスを
  当てたときに出る正確な数から読みます**（0.2.1から）。画面の表示が `1.05M`・`27.5K` のように
  省略されていても、`1,053,339`・`27,534` が入ります。正確な数がどこにも無いときは、その欄を入れません
- **今週は読みません。** 統合小説執筆環境の記録の粒度は日・月・年・累計で、週がありません。無い粒度を
  月や日に寄せると、あとから見て何の数字か分からなくなります
- 読めなかった欄は入れません（`0` を書きません）
- **`source`（どこで読んだか。0.6.0から）**：Narou.fun の作品ページから読んだ封筒だけに
  `"source": "narou.fun"` が付き、`site` は `"narou"`、`workId` は URL の Nコードです。
  **`source` の無い封筒は「そのサイトの管理画面そのもの」**の意味で、カクヨムの封筒は 0.5.0 までと同じ形です。
  統合小説執筆環境は、なろう本体の封筒（`source` 無しの `"narou"`）を断り続け、Narou.fun の封筒だけを受けます。
  この欄を知らない古い統合小説執筆環境は、Narou.fun の封筒を「なろうだから」と断ります（取り込みはされません）
- **`readAtBasis`（記録の日時が何の日時か。0.7.0から）**：Narou.fun の封筒だけに付きます。
  `"fetched"` はページの最終取得日時を読めた、`"clicked"` は読めずに押した時刻へ落とした、の意味です。
  欄の無い封筒（カクヨム）は、これまでどおり押した時刻です
- **日ごとの増減の行（0.7.0から）**：Narou.fun の日ごとの表から作る `period: "day"` の行は、
  `bookmarks`・`narou_ratingPoints` が**マイナスのこともあります**（前の日より減った日）。
  統合小説執筆環境がマイナスを受けるのは、この2つの欄の日ごとの行だけです

---

## 各サイトの規約について（要旨）

2026-09-05に各サイトの利用規約の原文を確かめた結果です（設計書6.79.1が正、ここは要旨）。

| サイト | 判定 |
|---|---|
| **カクヨム** | 自動化・収集を禁じる条文がなく、負荷も妨害も生まない貼り込みは**問題なし**。第1弾で対応 |
| **アルファポリス** | 同じく明示条文なし。**問題なし**。第1弾で対応 |
| **小説家になろう** | 第14条23号が「APIを使う以外の**自動化された手段でのアクセス・データ収集**」を禁止。
貼り込み（欄を埋めるだけ）は文言上その対象外と読めますが、**4サイトで最も厳しい書きぶり**で、
**送信の自動化は明確に一線を越えます**。表に枠だけ置き、**貼り込みは行いません**。
読み取り（アクセス解析の取り込み）は**対応しない**と決めています。
**なろうの数は、分析サイト Narou.fun のご自分の作品のページから読みます**（0.6.0。別のサイトで、
robots.txt は全許可・サイト自身に「クリップボードにコピー」の釦があります。利用規約の頁は見つかっていません） |
| **note** | 規約本文が機械取得を拒む（403）ため原文未確認。人の目で確かめるまで対応しません |
| **pixiv小説・ハーメルン** | 「機械的な操作」「ツール使用」の語があり**グレー**。対応するかは作者の最終判断 |

この拡張が「自動化されたアクセス」に当たらないと考える理由は、**サーバーへ何も送らない**からです。
ページを開くのも、投稿ボタンを押すのも、作者本人の操作です。

---

## 中身の構成

```
novel-post-helper/
├─ manifest.json      権限と、入るページの範囲
├─ popup.html/.css/.js  ボタンの画面。クリップボードを読み、照合し、ページへ依頼する
├─ common/            画面にもページにも依存しない、素の判定（テストがあるのはここ）
│  ├─ envelope.js     封筒の検証（版・サイト・題・本文・作品ID）
│  ├─ match.js        貼り込み先の照合（ドメイン・作成画面か・作品ID）
│  ├─ pageState.js    開いている画面の見立て（どちらのボタンが使える画面か）
│  ├─ guard.js        ログイン画面の検知、書き込んでよい欄かの判定
│  └─ messages.js     日本語の文言（1か所にまとめる）
├─ content/
│  ├─ sites.js        ★貼り込みの欄の指定。**直すのはここだけ**
│  ├─ statsSites.js   ★読み取りの表（どのページを・どう読むか）。**直すのはここだけ**
│  ├─ fill.js         ページの欄を埋める場所（送信はしない）
│  └─ read.js         唯一、管理画面の数字を読む場所（ページには1文字も書かない）
└─ test/              vitest。DOM操作そのものは対象外（実機の領分）
```

`common/` と `content/sites.js` は、ポップアップとページ側の**両方が同じファイルを読みます**
（写しを作らないため）。ビルド工程はありません——読み込んで、そのまま動きます。

### テスト

```powershell
npm install
npm test
```

`test/redLine.test.js` は、ソースを走査して「通信のコードが無いこと」「送信のコードが無いこと」
「Cookie に触れていないこと」「文字列からコードを作っていないこと」「HTMLを組み立てて差し込んで
いないこと」「ページへコードを注入していないこと」「ページと窓越しにやり取りしていないこと」
「権限が増えていないこと」を毎回確かめます。

0.2.0 で読み取りを足したので、「ページから集めない」の見張りも足しました——**封筒を組み立てるのは
`content/read.js` だけ**／**ページの文字を読むのは読み取り係と貼り込み係だけ**／**読んだものの
行き先はクリップボードだけ**（保存の口を使わない）／**ページを見張らない**。
ここに3つ目のファイルが増えたら、それは一線の引き直しです。

`test/readEnvelope.test.js` は、統合小説執筆環境のソース（`novel-ai-assistant`）がこのフォルダーの隣にあるときだけ、
**統合小説執筆環境の本物の読み口**（`core/readerStatsEnvelope.ts`）に封筒を食わせて確かめます。
封筒は2つのプロジェクトの約束事なので、検証を写すのではなく、相手に直接読ませています
（統合小説執筆環境のソースが無い環境では、その組だけ飛ばします）。

走査するファイルは**その場でフォルダーを辿って集めます**（`node_modules`・`test`・`.git` 以外の
すべて）。手で書いた一覧にすると、新しく足したファイルが検査されないまま通ってしまうためです。
**走査そのものが働いているか**——新しいファイルを拾うか、禁止の書き方を本当に見つけられるか——も、
架空のソースを食わせて同時に確かめています。

---

## 実機で確かめること（実機未確認の一覧）

### 済んだもの（2026-09-22、作者の Chrome の画面を開発の場から読んだ）

- [x] **カクヨムのタイトル欄・本文欄のセレクタ**（`content/sites.js`）
      → `input#episodeTitle-input[name="title"]`／`textarea#episodeBody-input[name="body"]`。
      **推測していた `name` の形は当たっていた**ので、`id` を先に、`name` をあとに残してある
- [x] **カクヨムの話の新規作成画面のURL** → `/my/works/{作品ID}/episodes/new` で合っていた
- [x] **カクヨムの投稿フォームの起点** → `form#episode-editForm`（`formScopes` の先頭に入れた）
- [x] **`content_scripts.matches` がカクヨムの投稿画面を覆えているか** → 覆えていた
      （0.2.0 で、アクセス数の画面 `https://kakuyomu.jp/works/*/accesses` を足した）
- [x] **本文欄が `textarea` か** → textarea だった。改行が消える心配は無い
- [x] **読者の反応のコピー（0.2.0）を、実際に押す** → 作品管理の画面で押せて、統合小説執筆環境の台帳へ
      3件（★1,612／今日のPV 1／今月のPV 667）入った。**ただし「読者からの反応」6欄のうち
      ★しか取れず**、残りは表示が省略形（`1.05M`）だったため落ちていた。0.2.1 で、数字に
      マウスを当てたときに出る正確な数（`data-ui-tooltip-label`）から読むよう直してある
- [x] **アクセス数の画面がページ送りか** → **50話ずつ**で、219話の作品は5ページだった。
      表の行はちょうど50行、「次へ」のリンクは `…/accesses?page=2`。0.2.2 で、次のページが
      あるときに「このページの分だけです」と出すようにした（リンクは押さない・開かない）

### 残っているもの

- [ ] **Narou.fun の作品ページで押して、7つの数（総合P・ブクマ・感想数・レビュー・評価P・評価者数・
      週間読者）が入るか確かめる**（0.6.0）。平均評価・評価頻度が**入らない**のが正しい姿。
      ページの形は開発の場で読んだHTMLに合わせたが、拡張を入れて押したことはまだ無い
- [ ] **Narou.fun の封筒を統合小説執筆環境が取り込めるか**（統合小説執筆環境の台帳に、その作品のなろうのNコードが要る）。
      **ほかの方の作品のページで押したときに、統合小説執筆環境が断るか**も確かめる
- [ ] **Narou.fun の日ごとの表を、拡張を入れて押して確かめる**（0.7.0）。表示件数10のときは
      「日ごと 9」と「表示件数を30に」の知らせ、30のときは「日ごと 29」で知らせが出ないのが正しい姿。
      統合小説執筆環境のグラフに「日ごとのブックマークの増減」が出て、減った日が0の線より下に赤で出るか
- [ ] **記録の日時が「最終取得日時」になっているか**（0.7.0）。統合小説執筆環境の履歴の日時と、メモの
      「日時は Narou.fun の最終取得日時」を見る。同じページを2度取り込んで、台帳が増えないことも
- [ ] **0.2.1 の直しを押し直して、「読者からの反応」6欄がすべて取れるか確かめる**
      （フォロワー・PV・★・レビュー・応援・コメント。★しか取れなかったのが直っているか）
- [ ] **作品管理の画面を押して、全話ぶん（219件）が1回で入るか確かめる**（0.4.0）。
      **いちばん確かめたいのは件数**——同じページに `tr.episode` を持つ控えの表がもう1つあり、
      拾うと438件（全話が二重）になる。統合小説執筆環境の台帳は追記なので、入ってしまうと見分けが付かない
- [ ] **作品管理の話ごとの欄が、応援・応援コメント・PV の3つとも入るか**。
      応援コメントの無い話は、**欄ごと入らない**（0にしない）のが正しい姿
- [ ] **アクセス数の画面を押して、話ごとの数と「次へ」の知らせが出るか確かめる**。
      2ページ目も同じように取り込んで、統合小説執筆環境の台帳が正しく積み上がるか（0.2.2）
- [ ] **「今日」「今月」のPVが、正しい数として読めるか**。「今週」は読まない作り
- [ ] **各話の更新日が入るか**（0.5.0）。`td.episode-date` の「2022年4月25日15:07 最終更新」を
      日本時間の日時にする。時刻の無い形・前に別の語がある形は**欄ごと入れない**作り
- [ ] **日ごとのPVが何日ぶん入るか**（0.5.0）。グラフの本数を実機で数えていない（30日ほどと見込み）。
      「今日 ◯ PV」と同じ日は1件にまとめる（表示の側を採る）
- [ ] **話数の読み取り**。「第N話」と書かれていない話は、表の順番で代える（それで合っているか）。
      作品管理の題は「第」が無く全角（`１話　転生`）で、**題の先頭にある数字だけ**を話数として読む
- [ ] **アルファポリスの投稿画面のURLと欄のセレクタ**。パスの見当が3通りあり、どれも未確認
- [ ] **アルファポリスの管理画面の形**。読み取りは表に枠だけ置いて止めてある（`supported: false`）
- [ ] **アルファポリスの作品IDの形**。URLで作者IDと作品IDが分かれる形があり、封筒のIDと
      どちらを突き合わせるべきか不明。分かったら `sites.js` の `workIdPatterns`（いまは空）へ書けば照合が戻る
- [ ] **アルファポリスの投稿フォームの起点セレクタ**（`sites.js` の `formScopes`）。欄探しはこの配下に
      限っている。起点が見つからないときはページ全体を探すが、その場合は当たった欄を「見当」扱いにして
      必ず確認を出す。また `manifest.json` の `matches` がその画面を覆えているか（覆えていないと拡張が入らない）
- [ ] **アルファポリスの本文の改行**が正しく入るか（欄が `contenteditable` の作りだった場合、
      改行が消える恐れがある。カクヨムは `textarea` だと確かめた）
- [ ] **画面が入力に気づくか**（React などで作られた画面では、値を入れても気づかれないことがある。
      ネイティブの setter 経由で `input`/`change` を配る対策を入れてあるが、実機で要確認）
- [ ] **クリップボードの読み書き**が、ポップアップで許可なしに通るか（`clipboardRead`／`clipboardWrite`
      で足りるはず）
- [ ] **確認ダイアログを出すとポップアップが閉じる**ため、結果はページの隅に数秒出る作りにしてある。
      その表示がサイトのCSSやCSPに邪魔されないか
- [ ] 別作品の投稿画面を開いた状態で押し、**取り違えが止まる**こと
- [ ] ログイン画面で押し、**何もしない**こと

# 統合小説執筆環境ヘルパー（novel-post-helper）

小説を書く方のための Chrome 拡張（Manifest V3）です。

**この拡張だけで使えます（0.10.0）。** ご自分の作品の読者の反応（PV・ブックマーク・評価など）を、
管理画面を開いたときに拡張の中へ記録し、作品ごとに**いまの数・日ごとの推移・離脱率／ブックマーク率／評価率**を
説明のページで簡単に集計します。使い方は「ご自分の作品の管理画面を開く → 作品を覚える → 集計を見る」の3つだけです
（下の「その0」）。グラフ・作品の事情を踏まえたAIの助言・原稿との突き合わせなど、詳しい分析は
VS Code の拡張機能 [「統合小説執筆環境」](https://marketplace.visualstudio.com/items?itemName=nonahisa.novel-ai-assistant)
（novel-ai-assistant）でできます。

**統合小説執筆環境をお使いの方は、説明のページの「統合小説執筆環境へ渡す」を入れてください（0.11.0）。**
新しく入れたときは切ってあり、切っている間はアイコンを押すと集計が開きます（VS Code は呼び出しません。
話の作成画面では、切っていても貼り込みます。0.11.1）。
0.10.0 以前から更新した方は、入ったままです。

統合小説執筆環境をお使いの方は、この拡張を**相棒**としても使えます。
統合小説執筆環境でコピーした原稿を、**作者が自分で開いた投稿画面の欄へ入れる**道具です。
0.2.0 から、**自分の管理画面に出ている読者の反応（PV・フォロワー・星・応援…）をコピーして
統合小説執筆環境へ戻す**こともできます。0.6.0 から、なろうの作品は**ご自分の作品の Narou.fun のページ**
（なろうの分析サイト `db.narou.fun`）からも読めます。

0.8.0 で名前を「貼り込み係」から **「統合小説執筆環境ヘルパー」** に改め、ポップアップを無くしました。
**ツールバーのアイコンを押すか、ページの上で右クリックして項目を選ぶだけで、いまの画面でできることを1つ実行します。**
使える画面ではアイコンに印（**貼**＝貼り込める、**読**＝読者の反応を読める）が付き、押した結果は Chrome のお知らせで出ます。

0.9.0 から、**読者の反応は、ご自分の作品の画面を開いたときに自動で拡張の中へ溜め、押したときにまとめて1回で渡します**
（アイコンの印は「読3」のように、溜まっている画面の数）。**ほかの方の作品は溜めません**——どなたの作品でも開ける画面
（Narou.fun・カクヨムのアクセス数）では、「ご自分の作品として覚える」を選んだ作品だけを溜めます。

> ## ⚠ いまの状態：テスト中
>
> - **カクヨムの投稿画面の欄は、2026-09-22 に実機で確かめました**（`form#episode-editForm`、
>   `#episodeTitle-input`、`#episodeBody-input`）。本文欄は `textarea` なので、改行はそのまま入ります
> - **カクヨムの読者の反応の読み取りは、2026-09-22 に実機で押しました。** 作品管理の画面から
>   コピーでき、統合小説執筆環境の記録へ3件（★1,612／今日のPV 1／今月のPV 667）入りました。ただし
>   このとき**「読者からの反応」6欄のうち★しか取れなかった**ので、0.2.1 で正確な数
>   （数字にマウスを当てたときに出る数）から読むよう直しました。**この直しを押し直しての確認は、まだです**
> - **話ごとの反応は、作品管理の画面から1回で全話ぶん読めます**（0.4.0から）。
>   **この画面での話ごとの読み取りは、まだ実機で押していません**
> - **各話の更新日と、直近の日ごとのPVも作品管理の画面から読みます**（0.5.0から。統合小説執筆環境の離脱率と
>   日ごとのグラフの材料です）。**こちらもまだ実機で押していません**
> - **アクセス数の画面は50話ずつのページ送りです**（219話の作品で5ページ）。1回のコピーで入るのは
>   その画面に出ている50話ぶんだけなので、次のページがあるときは「このページの分だけです」と出ます（0.2.2）。
>   ページごとにコピー→取り込みを繰り返してください（統合小説執筆環境は追記するので、二重にはなりません）。
>   **作品管理の画面から読めるようになったあとも、アクセス数の画面から読む方法は残してあります**（片方の画面の作りが変わっても、もう片方で読めるように）
> - **アルファポリスは、投稿も読み取りも実機未確認です。** 欄の指定は推測のままで、
>   読み取りは表に枠だけ置いて止めてあります
> - **Narou.fun の作品ページの読み取り（0.6.0）は、まだ実機で押していません。** ページの形は
>   2026-09-23 に開発の作業中に読んだ実物のページに合わせてあります（サーバーが組んだHTMLの、見出しと数の組の並び）
> - **0.8.0 の形（アイコン・右クリックだけで実行、アイコンの印、お知らせ、読み取りのあとに VS Code を呼ぶ）は、
>   まだ Chrome に入れて押していません。** 作り物の Chrome の中で、つなぎ方だけを確かめてあります（`test/background.test.js`）
> - **0.9.0 の「開いたら溜めて、まとめて渡す」は、まだ Chrome に入れて押していません。** 作り物の Chrome の中で、
>   溜まる・溜まらない・置き換わる・束の形・渡したら空、を確かめてあります（`test/background.test.js`・`test/stash.test.js`）。
>   **統合小説執筆環境の側が束（`novelai-stats-bundle`）を受け取れるようになるまでは、渡しても取り込まれません**
> - **0.10.0 の「記録と簡易集計」は、まだ Chrome に入れて開いていません。** 記録の畳み方・上限・率の式
>   （統合小説執筆環境の計算に同じ入力を通して、同じ値になること）は `test/history.test.js`、開いたら記録される・
>   他人の作品は記録しない・渡しても消えないは `test/background.test.js` で確かめてあります。
>   **集計の表の字の揃い方（全角と半角の幅）は、画面で見ていません**
> - **0.11.0 の「統合小説執筆環境へ渡す」の切り替えは、まだ Chrome に入れて押していません。** 切っているときに押すと集計が開く・
>   話の作成画面では切っていても貼り込む（0.11.1）・
>   溜まりに溜めず記録にだけ残す・印を出さない・新しく入れたときは切ってあり更新したときは入ったまま、は
>   `test/background.test.js`・`test/settings.test.js` で確かめてあります
> - **Narou.fun の日ごとのブクマと評価の表・最終取得日時（0.7.0）も、拡張を入れて押したことはまだありません。**
>   2026-09-23 に、ブラウザで組まれたあとの実物のページへ、読者の反応を読む部分をそのまま読み込ませて試し、
>   表示件数30で29日ぶん（減った2日を含む）が、ページの元の数と一致して読めました
>
> 欄や数字が見つからないときは「ページの形が変わったようです」と言って**何もしません**ので、
> 書きかけの原稿が壊れることも、読んでいない数字が統合小説執筆環境へ渡ることもありません。サイトの画面が変わったときに
> 直すのは、[`content/sites.js`](content/sites.js)（貼り込む欄の場所）と [`content/statsSites.js`](content/statsSites.js)（読む数の場所）の
> **2つのファイルだけ**です。
>
> 確かめが要ることは、この README の末尾「実機で確かめること」に並べてあります。

---

## この拡張がしないこと

統合小説執筆環境の「サイトへHTTPを1行も書かない」（設計書6.68）と同じ思想です。**約束ではなく、コードで守ります**
（しないことを見張るテスト `test/redLine.test.js` がプログラムの中身を調べて、下のことを毎回確かめます）。

1. **通信を1本もしません。** `fetch`・`XMLHttpRequest`・`WebSocket`・ビーコンのたぐいを1行も書いていません。
   動くのは、作者が自分で開いたページの中だけです。
   読者の反応を渡したあとに VS Code を呼び出しますが（0.8.0）、これは**お手元の VS Code を
   `vscode://` のリンクで前に出すだけ**で、どこのサーバーへも何も送りません。リンクにはデータを載せず、
   読者の反応はクリップボードで渡します。
   **読者の反応を溜める場所（0.9.0）は、この拡張の中の保存（`chrome.storage.local`）だけです。**
   Chrome が拡張ごとにパソコンの中に持つ保存場所で、**通信ではありません**。Google のアカウントで別のパソコンへ
   同期する保存（`chrome.storage.sync`）は使いません（使うと送ることになるため。テストが見張ります）。
   **集計のための記録（0.10.0）も同じ保存場所だけ**で、集計の計算も表と棒も拡張の中で組みます
   （外のライブラリや字体を読み込みません）。
   **説明のページには、統合小説執筆環境の Marketplace のページへのリンクが1本だけあります（0.10.0）。**
   押したときにだけブラウザが新しいタブで開くふつうのリンクで、この拡張がデータを送るのではありません
   （リンクは固定の文字で作品IDや数を載せず、どのページから来たかも渡しません）。外を指すのはこの1本だけで、
   テストが見張ります
2. **投稿ボタンを絶対に押しません。** クリックや送信を起こすコードがありません。欄を埋めたところで終わりで、
   内容を確かめて投稿するのは作者です（なろうの規約が明確に禁じるのは、この「自動投稿」です）
3. **認証情報に触れません。** パスワード欄のあるページでは、欄を探す前に降ります（「ログイン画面では動きません」）。
   Cookie もセッションも読み書きしません。ログインは、いつもどおりブラウザで作者が行ってください
4. **読むのは、ご自分の管理画面と、ご自分の作品の Narou.fun のページに出ている「ラベルと数の組」だけです**
   （設計書6.79.7の枠）。貼り込みのときに見るのは「埋める欄が在るか」「その欄が空か」だけ。読み取りのときも、
   本文・他の方の作品・ページ全体の文字は読みません（読む文字には長さの上限を掛けてあります）。
   集めてよいのは [`content/read.js`](content/read.js) だけで、**読んだものの行き先は、拡張の中の溜まり・記録と
   クリップボードだけ**です（0.9.0 から溜まり、0.10.0 から集計のための記録が加わりました）——どこへも送りません。対応するのはカクヨムと Narou.fun の作品ページだけで、
   **なろう本体（syosetu.com）とそのアクセス解析（KASASAGI）**・pixiv・ハーメルン・note では読み取りを
   行いません（アルファポリスは規約上は可ですが、画面を実機で確かめるまで止めてあります）。
   **Narou.fun とカクヨムのアクセス数の画面は、どなたの作品のページでも開けます**（カクヨムのアクセス数は、
   ログインしていなくても開けることを 2026-09-23 に確かめました）。この拡張には作者の作品かどうかが分からないので、
   **初めての作品は溜めず、押したときに「ご自分の作品ですか？」とお尋ねし、「覚える」を選んだ作品だけを溜めます**（0.9.0）。
   カクヨムの作品管理はご本人しか開けないので、開いた作品は自動で覚えます。取り込むときにも、統合小説執筆環境が
   作品ID（Nコード）でご自分の作品かどうかを確かめます
5. **開いただけでは貼り込みません。** 貼り込みは、アイコンを押すか右クリックの項目を選んだときに、1回だけ動きます。
   **開いただけで読むのは、ご自分の作品の読者の反応の画面だけ**で（0.9.0。作者の依頼「開いたら自動で溜める」）、
   開いてから少し待って1回だけ読みます。ページを見張る仕組み（MutationObserver・setInterval）は1行もありません。
   ほかの画面では、**開いたことだけ**を拡張の中へ知らせて、アイコンに印を付けます（0.8.0。ページの中は見ません）
6. **ページの中に、表示や部品を差し込みません**（0.8.0）。押した結果は Chrome のお知らせで出します。
   0.7.x までは結果をページの隅に数秒出していましたが、それもやめました
7. **勝手に新しいタブや窓を開きません**（0.8.0）。VS Code を呼ぶときも、いまのタブのまま呼びます。
   開くのは、入れた直後の説明のページ（1回だけ）、アイコンの右クリックで「読者の反応の集計を見る」を選んだときの
   説明のページ、「統合小説執筆環境へ渡す」を切っているときにアイコンを押したときの説明のページ（0.11.0）、
   説明のページの統合小説執筆環境へのリンクを押したときだけです（どれも作者の操作）

### 権限（できるだけ少なく）

| 権限 | 何のために |
|---|---|
| `clipboardRead` | 統合小説執筆環境がコピーしたデータを読むため |
| `clipboardWrite` | 読んだ読者の反応を、統合小説執筆環境へ渡すためにクリップボードへ置くため（0.2.0で追加） |
| `activeTab` | アイコンや右クリックを押したタブのURLを見て、貼り込み先・読み取り先が合っているか確かめるため |
| `notifications` | 押した結果を Chrome のお知らせで出すため（0.8.0で追加。ページの中には出さないため） |
| `contextMenus` | 右クリックの項目のため（0.8.0で追加）。0.10.0 から、アイコンの右クリックの「読者の反応の集計を見る」にも使います（権限は増えていません） |
| `storage` | ご自分の作品の読者の反応を、拡張の中（`chrome.storage.local`）へ溜めるため（0.9.0で追加。作者の依頼）。0.10.0 から、集計のための記録も同じ場所に置きます（権限は増えていません）。拡張の中の保存で通信ではありません。使うのは local だけで、同期（sync）は使いません。溜める量・記録する量には上限があります（下の「溜まりの上限と消し方」「記録の上限と消し方」） |
| `offscreen` | クリップボードの読み書きのため（0.8.0で追加）。ポップアップが無くなったので、押したときだけ画面に出ない拡張のページを開き、そこで読み書きして閉じます。投稿サイトのページには入りません |
| `content_scripts` の `matches` | カクヨムの投稿まわり・管理画面と、アルファポリスの投稿まわりのページ、Narou.fun の作品ページ（`https://db.narou.fun/works/*`。0.6.0）にだけ入るため。0.9.0 で、アクセス数の2ページ目以降（`https://kakuyomu.jp/works/*/accesses?*`）を足しました——Chrome の範囲の指定は `?` から後ろも含めて照合するので、`…/accesses` だけでは `?page=2` に入っていませんでした |

`host_permissions` は**書いていません**。ページへ入る範囲は `manifest.json` の `content_scripts.matches`
だけが決めており、そこに書いていないサイトでは、この拡張は存在しないのと同じです。
全部のタブのURLが読める `tabs` も**足していません**（0.8.0）。アイコンの印は、上の範囲のページからの
「開いた」の知らせで付けます。

---

## 導入のしかた（開発者モードで読み込む）

Chrome ウェブストアでは、まだ配っていません（配る予定です。ストアから入れたときも、入れた直後に
「はじめに」の説明のページが開きます）。いまは手元のフォルダーをそのまま読み込みます（ビルドは要りません）。

1. Chrome で `chrome://extensions` を開く
2. 右上の **「デベロッパー モード」** をオンにする
3. **「パッケージ化されていない拡張機能を読み込む」** を押し、このフォルダー（`novel-post-helper`）を選ぶ
4. ツールバーに「統合小説執筆環境ヘルパー」のアイコンが出る（見当たらなければ、パズルのかけらの印から固定する）
5. 初めて入れたときだけ、説明のページ（「はじめに」の3つの手順・集計・「この拡張がしないこと」）が開く。
   あとから見るときは、アイコンを右クリックして「読者の反応の集計を見る」（または「オプション」）

**直したあとは、`chrome://extensions` の再読み込みボタンを押し、投稿ページも再読み込みしてください。**
ページの中で動く部分は、ページを開いたときに入るためです。

---

## 使い方の流れ

**アイコンを押すか、ページの上で右クリックして「統合小説執筆環境ヘルパー：…」を選ぶと、いまの画面でできることを
1つだけ実行します**（0.8.0から。それまではポップアップの2つのボタンでした）。1つの画面で両方ができることは無いためです。

| アイコンの印 | 画面 | 押すと | 右クリックの項目 |
|---|---|---|---|
| **貼**（青） | 話の作成画面 | 原稿を題と本文の欄へ入れる | 統合小説執筆環境ヘルパー：この画面に貼り込む |
| **読3**（緑） | ご自分の作品の作品管理・アクセス数・Narou.fun の作品ページ | この画面を読み直してから、溜まった読者の反応をまとめて渡し、VS Code を呼ぶ（0.9.0。数字は溜まっている画面の数。溜まりが無いときは **読**） | 統合小説執筆環境ヘルパー：読者の反応をまとめて渡す |
| **?**（橙） | まだ覚えていない作品の Narou.fun のページ・カクヨムのアクセス数 | 「ご自分の作品ですか？」と訊く（0.9.0） | 統合小説執筆環境ヘルパー：この作品を自分の作品として覚える |
| （**読3** がどの画面にも出る） | それ以外の画面で、溜まりがあるとき | 溜まった読者の反応をまとめて渡し、VS Code を呼ぶ（0.9.0） | 統合小説執筆環境ヘルパー：溜まった読者の反応をまとめて渡す（この拡張の入るページだけ） |
| （無し） | それ以外 | どの画面でなら何ができるかを知らせる | 出ない |

上の表は「統合小説執筆環境へ渡す」を**入れているとき**です。**切っているとき**（新しく入れたときはこちら。0.11.0）は次のとおりです。

| アイコンの印 | 画面 | 押すと | 右クリックの項目 |
|---|---|---|---|
| **読**（緑。数字は付かない） | ご自分の作品の作品管理・アクセス数・Narou.fun の作品ページ | この画面を読み直して記録し、説明のページの集計を開く | 統合小説執筆環境ヘルパー：読者の反応の集計を見る |
| **?**（橙） | まだ覚えていない作品の Narou.fun のページ・カクヨムのアクセス数 | 「ご自分の作品ですか？」と訊く | 統合小説執筆環境ヘルパー：この作品を自分の作品として覚える |
| **貼**（青） | 話の作成画面 | 原稿を題と本文の欄へ入れる（入れているときと同じ。0.11.1） | 統合小説執筆環境ヘルパー：この画面に貼り込む |
| （無し） | それ以外 | 説明のページの集計を開く | 出ない |

切っている間は、開いた画面の読者の反応を集計のための記録にだけ残し、統合小説執筆環境へ渡す分（溜まり）には溜めません。
切る前に溜まっていた分は消えません（入れ直せば渡せます）。印は、開いているページを開き直すと切り替わります。

押した結果は、画面の隅に出る **Chrome のお知らせ**で出ます（ページの中には出しません）。お知らせが出ないときは、
Windows の「設定」→「通知」で Google Chrome の通知がオンか確かめてください。
押したあとの確かめ（作品の取り違え・読める管理画面か・ログイン画面か）は、これまでどおり全部そのまま働きます。
印は見た目の親切で、守りではありません——印が古いままでも、間違った場所へは貼り込まれません。

印と右クリックの項目は、**拡張を入れ直したあと、ページを読み直すまで出ません**（ページが開いたときに付けるため）。

### その0　読者の反応を記録して、簡単に集計する（この拡張だけで使えます。0.10.0）

統合小説執筆環境（VS Code）を持っていなくても使えます。

1. **ご自分の作品の管理画面を開く。** カクヨムは作品管理の画面（`kakuyomu.jp/my/works/{作品ID}`）、
   なろうの作品は Narou.fun の作品のページ（`db.narou.fun/works/{Nコード}`）
2. **作品を覚える。** カクヨムの作品管理はご本人しか開けないので、開いただけで自動で覚えます。
   Narou.fun のページ・カクヨムのアクセス数はどなたの作品でも開けるので、アイコンに **?** が付きます。
   押して「覚える」を選ぶか、説明のページの「ご自分の作品として覚えた作品」に作品ID（Nコード）を書いて保存します
3. **集計を見る。** 覚えた作品の画面を開くたびに記録します。アイコンを押す（「統合小説執筆環境へ渡す」を切っているとき）か、
   アイコンを右クリック → **「読者の反応の集計を見る」** で、説明のページの「読者の反応の集計」が開きます

集計に出るもの（覚えた作品ごと）：

| 欄 | 中身 |
|---|---|
| いまの数 | サイトが見せている作品全体の数（カクヨム：PV・フォロワー・★・レビュー・応援・応援コメント／Narou.fun：総合P・ブクマ・評価P・評価者数・レビュー・感想数・週間読者）。今日と今月の数があれば、それも |
| 日ごとの数 | サイトが出している、その日の数（カクヨムの日ごとのPV、Narou.fun の日ごとのブクマと評価Pの増減）。直近14日を、字の棒つきで |
| 記録した日ごとの数 | この拡張が記録した日ごとの作品全体の数と、前の記録からの増え方。記録の間が1日より空いた日は `*` を付けます（その間の合計の増え方なので） |
| 率 | 離脱率・ブックマーク率・評価率。**式と実際の数を並べます** |

**率の式は統合小説執筆環境と同じです**（`src/core/readerRates.ts`）。

- **離脱率** ＝ 1 − 基準の話のPV ÷ 第1話のPV。基準の話は、更新から**読んだ時点までに72時間以上たっていた**話のうち、話数がいちばん大きい話
  （更新した直後の話はまだ読まれ切っていないので、そこで測ると離脱率が高く見える）
- **ブックマーク率** ＝ 作品全体のブックマーク（カクヨムはフォロワー）÷ 第1話のPV
- **評価率** ＝ 評価した人数 ÷ 第1話のPV（カクヨムはレビュー、なろうは評価者数——なろうの「レビュー」は書かれたレビューの件数のため）

材料が欠けたら **0% とは書かず、「出せません：第1話のPVがありません」のように足りない材料を書きます**
（0% と書くと、読めなかったのか本当に0なのか見分けが付かなくなるため）。
話ごとのPVは、カクヨムの作品管理（全話・更新日つき）かアクセス数の画面から読みます。
**Narou.fun には話ごとのPVが無いので、なろうの作品の率は出せません**（それも理由として出ます）。

グラフ・作品の事情を踏まえたAIの助言・原稿との突き合わせなど、詳しい分析は統合小説執筆環境でできます。
集計の下に、[統合小説執筆環境の Marketplace のページ](https://marketplace.visualstudio.com/items?itemName=nonahisa.novel-ai-assistant)
へのリンクがあります（押したときだけ新しいタブで開きます。この拡張がデータを送るのではありません）。

> 下の「その1」「その2」は、**統合小説執筆環境をお使いの方向け**です。「その1」の貼り込みは、
> 説明のページの「統合小説執筆環境へ渡す」の入・切にかかわらず使えます（0.11.1）。「その2」を使うときは、説明のページ
> （アイコンを右クリック →「読者の反応の集計を見る」）の **「統合小説執筆環境へ渡す」** を入れてください（0.11.0）。
> 切っている間は「まとめて渡す」をせず、VS Code も呼び出しません。

### その1　原稿を投稿画面へ入れる（統合小説執筆環境をお使いの方向け）

1. **統合小説執筆環境（VS Code）** で、投稿する話を開き、投稿キットの **ヘルパーへ渡す形のコピー**
   （統合小説執筆環境の表示が「貼り込み係へ渡す形でコピー」のときは、それ）を実行する
2. **ブラウザ** で、その作品の**話の新規作成画面**を開く（ログインは普通に済ませておく）。アイコンに **貼** が付く
3. ツールバーのアイコンを押す（または、ページの上で右クリックして「統合小説執筆環境ヘルパー：この画面に貼り込む」）
4. タイトルと本文が入り、「貼り込みました」のお知らせが出る。**内容を自分の目で確かめて、投稿ボタンは自分で押す**

途中で止まったときは、理由がそのまま出ます。

| 出る文 | 意味 |
|---|---|
| クリップボードに貼り込み用のデータがありません | 統合小説執筆環境でのコピーがまだ／別のものをコピーした |
| この画面は◯◯の投稿画面ではないようです | サイトが違う（コピーしたデータはカクヨム宛て、開いているのは別サイト等） |
| この画面は作品「…」の投稿画面ではないようです | **別の作品**の投稿画面を開いている（複数タブのときに起きる） |
| ◯◯の話の作成画面ではないようです | 作品の管理画面など、欄のないページを開いている |
| ログイン画面では動きません | セッションが切れている。ログインし直してから |
| ページの形が変わったようです | 入れる欄が見つからなかった。サイトの画面が変わったのかもしれません。ヘルパーを新しくするか、開発者へお知らせください |

貼り込む前に確認を出すのは、次の2つのときです。断れば何もしません。

- **欄にすでに文が入っている**とき（書きかけの前書きを黙って消さないため）
- **その欄が「見当を付けた欄」だった**とき。`#body` や `contenteditable` のような、
  どのページにもあり得る形で当たった場合は、投稿の欄でない恐れがあるので、空でも一度お尋ねします
  （サイト固有の形——`textarea[name="body"]` など——で当たり、かつ空のときだけ、黙って入れます）

どの欄へ入れたのかは、結果の表示に `本文欄（textarea#episode-body）` の形で出ます。
見覚えのない欄の名前が出たら、貼り込み先が違っている合図です。

### その2　読者の反応を統合小説執筆環境へ戻す（統合小説執筆環境をお使いの方向け。0.2.0から。いまはカクヨムと、なろうの作品の Narou.fun）

**0.9.0 から、開いたら溜まり、押すとまとめて1回で渡します**（作者の依頼「キャッシュして渡すことはできないでしょうか？」）。

1. **ブラウザ** で、ご自分の作品の読者の反応の画面を開く。**開くだけで、その画面の読者の反応が拡張の中へ溜まります**
   （アイコンの印が「読1」「読2」…と増えます）。いくつもの作品・ページを続けて開いて構いません
2. **初めての Narou.fun の作品・カクヨムのアクセス数**では、印が **?** になり、開いても溜まりません。
   アイコンを押すと「ご自分の作品ですか？」と Chrome のお知らせで訊くので、「覚える」を押す（以後はその作品を開くと溜まります）。
   カクヨムは、**作品管理の画面を一度開けば、その作品を自動で覚えます**（作品管理はご本人しか開けないため）
3. 溜まったら、**アイコンを押す**（どの画面でも構いません。読者の反応の画面で押したときは、その画面を読み直してから）。
   溜まった分が1つにまとまってクリップボードへ入り、VS Code が前に出て、統合小説執筆環境が取り込みます
   （「Visual Studio Code を開きますか？」と Chrome が尋ねたら「開く」）
4. 渡すと溜まりは空になります（印が消えます）。統合小説執筆環境が取り込めなかったときは、アイコンを右クリック →
   「オプション」の説明のページで **「もう一度渡す」** を押す（渡した分は、次に渡すまで・長くても7日、控えてあります）

以下は、読める画面と、1つの画面から読むものの説明です（0.8.0 までと同じ）。

1. 読める画面は次のどちらか（ご自分の作品の画面です）。
   **作品管理のほうが、1回で全部入ります**（0.4.0から）

   | | 作品管理 | アクセス数 |
   |---|---|---|
   | URL | `kakuyomu.jp/my/works/{作品ID}` | `kakuyomu.jp/works/{作品ID}/accesses` |
   | 話の数 | **1回で全話** | **50話ずつ**・ページ送り（219話なら5ページ） |
   | 話ごとに読む欄 | 応援・応援コメント・PV | 応援・PV |
   | 作品全体の反応 | 読む（フォロワー・PV・★・レビュー・応援・応援コメント）＋今日と今月のPV | 読まない |
   | 各話の更新日 | 読む（「最終更新」の日時。0.5.0から） | 読まない |
   | 直近の日ごとのPV | 読む（反応のグラフの1本ずつ。0.5.0から） | 読まない |
   | 押す回数 | **1回** | ページの数だけ |

2. その画面で押したときのお知らせには、その画面からの件数も出ます
   （「この画面からは 251件（作品全体 2・日ごと 30・話ごと 219）」。作品全体の2は、反応の6欄と今月のPV）
3. VS Code が前に出ないとき・統合小説執筆環境が古いときは、作品管理メニューの
   **「読者の反応を貼り付けて取り込む」** を実行する（読者の反応はクリップボードに入っています）

読むのは**そのとき画面に出ている数だけ**です。月を切り替えたり、次のページへ進んだりは
しません（別の期間が欲しいときは、ご自分で画面を切り替えて、もう一度押してください）。

**アクセス数のページに「次へ」があるときは、そう出ます**（0.2.2から）。0.9.0 からは、「次へ」で次のページを
開けば、そのページも溜まります（ページごとに別の1件。同じページを開き直すと置き換わるので、**二重にはなりません**）。

| 出る文 | 意味 |
|---|---|
| この画面はこのページの分だけです。「次へ」で次のページを開くと… | 話が50話を超えていて、次のページがある |
| 日ごとの表は、画面に出ている行の分だけです。表の下の「表示件数」を30に… | Narou.fun の日ごとの表が10行（または20行）のまま（0.7.0。開いたときに溜めた分も10行のままなので、30にしてからもう一度押す） |
| 溜まっている読者の反応がありません | 説明のページで「まとめて渡す」を押したが、溜まりが空（0.9.0） |
| まとめましたが、クリップボードへ置けませんでした | 溜まりは消えていないので、もう一度押せば渡せる（0.9.0） |
| この画面は、読者の反応を読める管理画面ではありません | 上の2つの画面ではない（話の編集画面や公開ページなど） |
| このサイトの読者の反応には対応していません | カクヨム・Narou.fun 以外のタブを開いている（なろう本体・KASASAGI もここ） |
| アルファポリスの読者の反応の読み取りには、まだ対応していません | 管理画面の形を実機で確かめられていないため止めてある |
| ページの形が変わったようです | 数字が1つも見つからなかった。サイトの画面が変わったのかもしれません。ヘルパーを新しくするか、開発者へお知らせください |
| ログイン画面では動きません | セッションが切れている |

読めなかった欄は、**0ではなく「欄ごと無し」**で渡します（実機の「応援コメント –」がこれです）。
0と書いてしまうと、統合小説執筆環境の記録に「読んでいない数字」が残り、あとから見分けられなくなるためです。

#### なろうの作品：Narou.fun の作品ページ（0.6.0から）

なろう本体（syosetu.com）は規約の判断で読みません。代わりに、なろうの分析サイト **Narou.fun** の
**ご自分の作品のページ**（`https://db.narou.fun/works/{Nコード}`）から、作品全体の数を読みます。
統合小説執筆環境の執筆量パネル「サイトの記録」の **「分析（Narou.fun）を開く」** で開けるページです。

1. **ブラウザ** で、ご自分の作品の Narou.fun のページを開く。初めての作品なら印が **?** になるので、アイコンを押して「覚える」を選ぶ（0.9.0）
2. ページの下の日ごとの表で **「表示件数」を30にする**（0.7.0。10のままだと9日ぶんしか入りません。
   開いたときに溜めた分は10行のままなので、30にしたら次の手順で読み直します）
3. アイコン（印は **読1** など）を押す。その画面を読み直して溜め、溜まった分をまとめて渡します（「この画面からは 30件（作品全体 1・日ごと 29）」）
4. VS Code が前に出て、**統合小説執筆環境** が取り込む（前に出ないときは **「読者の反応を貼り付けて取り込む」** を実行する）

| Narou.fun の表示 | 統合小説執筆環境の欄 |
|---|---|
| 総合P | `points`（評価） |
| ブクマ | `bookmarks`（ブックマーク） |
| 感想数 | `comments`（コメント） |
| レビュー | `reviews`（レビュー） |
| 評価P | `narou_ratingPoints`（評価ポイント） |
| 評価者数 | `narou_raters`（評価者数） |
| 週間読者 | `narou_weeklyReaders`（週間読者） |

**読まないもの**：

- **平均評価**——評価P ÷ 評価者数（10点満点の点の平均）で、なろうのバックアップの「評価平均」（星の平均）と
  尺度が違います。同じ欄へ入れると数字が化けるので読みません（評価Pと評価者数から割れます）
- **評価頻度**——評価者数 ÷ ブクマ で、統合小説執筆環境の**評価率（評価した人数 ÷ 第1話のPV）とは別物**です
- 日間P・日間イン・ベスト（順位まわり）、全話数・最終更新・字数（読者の反応ではない）、あらすじ・題
- 「解析」の行き先（**KASASAGI**。なろうの運営会社のアクセス解析）——なろう本体と同じ扱いで、辿りません

**日ごとのブクマと評価の表（0.7.0から）**：表の数は**その日までの累計**なので、**前の日との差**を
その日の数にします（ブクマ→`bookmarks`、評価→`narou_ratingPoints`。表の「評価」は評価Pです）。

- **ブクマが外された日は、マイナスのまま**統合小説執筆環境へ渡します（そちらのグラフでは0の線より下に赤で出ます）
- **表の最初の日（前の日が無い日）は入れません**。差が取れないためです（翌日以降の取り込みで埋まります）
- 括弧の中の差（`(+1)`）は読みません。累計どうしから自分で取ります（最初の行の「(0)」はサイトが置いた値です）
- 日付に年が無いので、**押した日から年を補います**（1月に押した12月の行は前の年）。
  押した日より先の日付の行（リアルタイム表示の「明日」）は入れません
- 表は10行ずつです。**「次へ」でページごとに取り込むのではなく、「表示件数」を30にしてください**
  ——ページの境目の日の差が取れずに抜けるためです。途中までのときは、コピーのあとにそう出ます

**記録の日時は、ページの「最終取得日時」です（0.7.0から）**。Narou.fun の数は、Narou.fun がなろうから
取ってきた時点のものなので、押した時刻ではなくこちらを使います。読めなかったときは押した時刻にし、
そうと分かる印をコピーするデータに付けます（統合小説執筆環境の記録のメモに出ます）。

**ほかの方の作品は溜めません**（0.9.0）。Narou.fun は誰の作品のページでも開けるので、この拡張には作者の作品かどうかが
分かりません。そこで、**「覚える」を選んだ Nコードの作品だけ**を、開いたときに溜めます。覚えた一覧は説明のページ
（アイコンを右クリック →「オプション」）で見られ、行を消せば外せます。取り込むときにも、統合小説執筆環境が作品ID（Nコード）で
ご自分の作品かどうかを確かめます（統合小説執筆環境にその作品のNコードを登録していないときは、登録するまで取り込みません）。

**率（離脱率・ブックマーク率・評価率）は、Narou.fun だけでは出ません。** 分母の「第1話のPV」が
Narou.fun に無いためです（KASASAGI にありますが、読みません）。

### クリップボードについて（正直に書きます）

Windows の「クリップボードの履歴」「デバイス間の同期」を有効にしていると、コピーした本文が
Microsoft のクラウドを経由することがあります。これは統合小説執筆環境の「投稿サイト用に変換してコピー」でも同じですが、
未公開のまま取りやめる本文も通りうる、ということは知っておいてください。

---

## 受け渡しのデータの形

統合小説執筆環境とこの拡張が、クリップボード越しにやりとりする JSON です（設計書6.79.3）。

```json
{
  "novelai-post": 1,
  "site": "kakuyomu",
  "workId": "16816927859000000000",
  "title": "第13話 星の落ちる夜",
  "body": "（変換済みの本文）"
}
```

- `novelai-post` は版。**1でなければ読みません**（知らない版を推測で読むと、欄の意味が変わっていたときに
  本文を取り違えて入れることになるため）
- `site` は `kakuyomu` / `narou` / `alphapolis`
- `workId` は任意。**あれば、投稿画面のURLの作品IDと突き合わせます**（複数作品の取り違え防止）。
  ただし**アルファポリスでは、いまは突き合わせません**——URL上の作品IDの形が実機で確かめられておらず、
  当てずっぽうの照合が「たまたま一致」すると、確かめた顔で別の作品へ貼り込むことになるためです。
  この場合は結果の先頭に【…ご自分で確かめてください】と出ます
- `title` は空でも構いません（空なら、タイトル欄は触りません）
- `body` は50万字まで。それを超えるものは、統合小説執筆環境の側の取り違え（作品まるごと等）とみなして断ります
- **知らない欄は無視して読みます。** 統合小説執筆環境が将来 `caption` などを足しても、古いヘルパーが壊れません

### 読者の反応のデータ（向きが逆。拡張 → 統合小説執筆環境）

こちらは、この拡張が作って**クリップボードへ置く**JSONです（設計書6.79.7）。統合小説執筆環境の
`core/readerStatsEnvelope.ts` が受けます。

```json
{
  "novelai-stats": 1,
  "site": "kakuyomu",
  "workId": "16816927859000000000",
  "readAt": "2026-09-22T01:00:00.000Z",
  "entries": [
    { "scope": "work", "metrics": { "bookmarks": 23, "pv": 1269, "points": 18, "reviews": 6, "likes": 33 } },
    { "scope": "work", "period": "day", "periodKey": "2026-09-22", "metrics": { "pv": 0 } },
    { "scope": "work", "period": "month", "periodKey": "2026-09", "metrics": { "pv": 2 } },
    { "scope": "episode", "episode": 1, "metrics": { "likes": 4, "pv": 102 } }
  ]
}
```

- `readAt` は**読んだ時刻**です（取り込んだ時刻ではありません）。Narou.fun から読んだデータだけは、ページの
  **最終取得日時**です（0.7.0から。下の `readAtBasis`）
- `workId` はURLから読んだ作品IDです。統合小説執筆環境はこれを作品ごとの記録と突き合わせ、**別の作品の画面を開いたまま
  押したとき**に取り込みを止めます
- カクヨムの表示を統合小説執筆環境の欄へ写す対応は、フォロワー→`bookmarks`、PV→`pv`、★の数→`points`、
  レビュー→`reviews`、応援→`likes`、応援コメント→`comments` です
- **「読者からの反応」の6欄（フォロワー・PV・★・レビュー・応援・コメント）は、数字にマウスを
  当てたときに出る正確な数から読みます**（0.2.1から）。画面の表示が `1.05M`・`27.5K` のように
  省略されていても、`1,053,339`・`27,534` が入ります。正確な数がどこにも無いときは、その欄を入れません
- **今週は読みません。** 統合小説執筆環境の記録の粒度は日・月・年・累計で、週がありません。無い粒度を
  月や日に寄せると、あとから見て何の数字か分からなくなります
- 読めなかった欄は入れません（`0` を書きません）
- **`source`（どこで読んだか。0.6.0から）**：Narou.fun の作品ページから読んだデータだけに
  `"source": "narou.fun"` が付き、`site` は `"narou"`、`workId` は URL の Nコードです。
  **`source` の無いデータは「そのサイトの管理画面そのもの」**の意味で、カクヨムのデータは 0.5.0 までと同じ形です。
  統合小説執筆環境は、なろう本体のデータ（`source` 無しの `"narou"`）を断り続け、Narou.fun のデータだけを受けます。
  この欄を知らない古い統合小説執筆環境は、Narou.fun のデータを「なろうだから」と断ります（取り込みはされません）
- **`readAtBasis`（記録の日時が何の日時か。0.7.0から）**：Narou.fun のデータだけに付きます。
  `"fetched"` はページの最終取得日時を読めた、`"clicked"` は読めずに押した時刻へ落とした、の意味です。
  この欄の無いデータ（カクヨム）は、これまでどおり押した時刻です
- **日ごとの増減の行（0.7.0から）**：Narou.fun の日ごとの表から作る `period: "day"` の行は、
  `bookmarks`・`narou_ratingPoints` が**マイナスのこともあります**（前の日より減った日）。
  統合小説執筆環境がマイナスを受けるのは、この2つの欄の日ごとの行だけです

### 溜めた分をまとめたデータ（束。0.9.0）

「まとめて渡す」でクリップボードへ置くのは、上の読者の反応のデータを**配列で束ねたもの**です。
統合小説執筆環境の取り込み口（`vscode://nonahisa.novel-ai-assistant/import-reader-stats`）が受けます。

```json
{
  "kind": "novelai-stats-bundle",
  "version": 1,
  "handedAt": "2026-09-23T05:10:00.000Z",
  "items": [
    { "novelai-stats": 1, "site": "kakuyomu", "workId": "16816927859000000000", "readAt": "…", "entries": [ … ] },
    { "novelai-stats": 1, "site": "narou", "source": "narou.fun", "workId": "N1234AB", "readAt": "…", "readAtBasis": "fetched", "entries": [ … ] }
  ]
}
```

| 欄 | 型 | 意味 |
|---|---|---|
| `kind` | 文字列 | いつも `"novelai-stats-bundle"`（束の目印） |
| `version` | 数 | いつも `1`（束の版。1でなければ読まない取り決め） |
| `handedAt` | 文字列（ISO 8601、UTC） | まとめて渡した時刻 |
| `items` | 配列 | 上の「読者の反応のデータ」（`novelai-stats` v1）そのものを、**溜めた順**（古いものが先）に並べたもの。1件は「1つの作品の1つの画面（アクセス数ならそのページ）」。空の束は作りません |

- `items` の1件ずつの形は 0.8.0 までと**まったく同じ**です。溜めるための印（どの画面か・いつ溜めたか）は束に入れません
- 同じ作品の作品管理とアクセス数が両方入ることがあります（話ごとの数が重なります）。統合小説執筆環境の取り込みは
  追記（同じ話の同じ数字は上書き）なので、1件ずつ順に取り込めば二重になりません

### 溜まりの上限と消し方（0.9.0）

- **上限は 50画面・200万字**（データの文字数の合計）。越えたら、**前に溜めたものから**消えます
- 同じ画面（同じ作品・同じ種類の画面・アクセス数なら同じページ）を開き直すと、**新しいほうで置き換わります**
- **渡すと溜まりは空になります。** 渡した分は「控え」として、**次に渡すまで（長くても7日）** 残ります。
  統合小説執筆環境が受け取れたかはこの拡張には分からない（通信しないので返事が来ない）ため、説明のページの
  **「もう一度渡す」** で同じものを渡し直せます
- 説明のページの **「溜まりを空にする」** で、渡さずに消せます。覚えた作品は、説明のページの欄から行を消して保存すると外れます
- 保存しているのは、溜まり・控え・覚えた作品（サイト・作品ID・覚えた日）・訊きかけの作品の4つと、
  0.10.0 からの集計のための記録（下）、0.11.0 からの「統合小説執筆環境へ渡す」の入・切（保存の鍵 `helperSettings`）だけです
- 「統合小説執筆環境へ渡す」を切っている間は、溜まりに溜めません。切る前に溜まっていた分は、そのまま残ります

### 記録の上限と消し方（0.10.0）

集計のための記録は、溜まりとは**別のもの**です（保存の鍵も別の `readerHistory`）。**統合小説執筆環境へ渡しても、
溜まりを空にしても消えません。**

- **覚えた作品だけ**を記録します。説明のページで覚えた作品から外すと、その作品の記録も消えます
  （外したのは、ご自分の作品ではなかったからかもしれないため）
- **同じ作品・同じ日の分は1つにまとめ、欄ごとに新しい数で置き換えます**（開き直すたびに増えません）。
  作品管理とアクセス数のように読める欄が違う画面は、欄ごとに合わさります。古い読み取りで新しい数を上書きしません
- 話ごとの数は、日ごとには残さず、**話ごとにいちばん新しい数だけ**を持ちます（率に要るのはそれだけで、
  219話の作品を日ごとに残すと、半年で作品1つが保存の持ち分を食い尽くすため）
- **上限は 20作品・180日（月ごとの数は24か月）・150万字**（記録全体の文字数）。越えたら、
  作品はいちばん長く記録していないものから、日はいちばん古い日から消えます（どの作品も最後の1日は残します）
- 説明のページの **「集計の記録を消す」** で、全部消せます（覚えた作品と溜まりは残ります）

---

## 各サイトの規約について（要旨）

2026-09-05に各サイトの利用規約の原文を確かめた結果です（設計書6.79.1が正、ここは要旨）。

| サイト | 判定 |
|---|---|
| **カクヨム** | 自動化・収集を禁じる条文がなく、負荷も妨害も生まない貼り込みは**問題なし**。第1弾で対応 |
| **アルファポリス** | 同じく明示条文なし。**問題なし**。第1弾で対応 |
| **小説家になろう** | 第14条23号が「APIを使う以外の**自動化された手段でのアクセス・データ収集**」を禁止。
貼り込み（欄を埋めるだけ）は文言上その対象外と読めますが、**4サイトで最も厳しい書きぶり**で、
**送信の自動化は明確に禁止の対象です**。表に枠だけ置き、**貼り込みは行いません**。
読み取り（アクセス解析の取り込み）は**対応しない**と決めています。
**なろうの数は、分析サイト Narou.fun のご自分の作品のページから読みます**（0.6.0。別のサイトで、
robots.txt は全許可・サイト自身に「クリップボードにコピー」の釦があります。利用規約の頁は見つかっていません） |
| **note** | 規約本文が機械取得を拒む（403）ため原文未確認。人の目で確かめるまで対応しません |
| **pixiv小説・ハーメルン** | 「機械的な操作」「ツール使用」の語があり**グレー**。対応するかは作者の最終判断 |

この拡張が「自動化されたアクセス」に当たらないと考える理由は、**サーバーへ何も送らない**からです。
ページを開くのも、投稿ボタンを押すのも、作者本人の操作です。

---

## 中身の構成

```
novel-post-helper/
├─ manifest.json      権限と、入るページの範囲
├─ background.js      裏方（service worker）。アイコン・右クリックを受け、照合し、ページへ依頼し、知らせる（0.8.0）。溜まりの保存に触れるのはここだけ（0.9.0）
├─ offscreen.html/.js 画面に出ない、クリップボードの受け渡しのページ（押したときだけ開いて閉じる。0.8.0）
├─ options.html/.css/.js  説明のページ（使い方と「この拡張がしないこと」。0.8.0）。溜まり・覚えた作品の一覧、もう一度渡す（0.9.0）。はじめにの手順・読者の反応の集計・統合小説執筆環境への案内（0.10.0）
├─ icons/             アイコン（統合小説執筆環境のアイコンから作った 16・32・48・128）
├─ common/            画面にもページにも依存しない、素の判定（テストがあるのはここ）
│  ├─ envelope.js     受け渡しのデータの検証（版・サイト・題・本文・作品ID）
│  ├─ match.js        貼り込み先の照合（ドメイン・作成画面か・作品ID）
│  ├─ pageState.js    開いている画面の判定（貼り込める画面か・読める画面か）
│  ├─ actions.js      判定から、アイコンの印・右クリックの項目・実行する処理・VS Code を呼ぶかを決める（0.8.0）
│  ├─ stash.js        溜めてよいか（他人の作品を溜めない）・同じ画面の置き換え・上限・束の形（0.9.0。保存には触れない）
│  ├─ history.js      集計のための記録の畳み方・上限・率の式・集計の文（0.10.0。保存には触れない）
│  ├─ settings.js     「統合小説執筆環境へ渡す」の入・切の決め方（新しく入れたら切る・更新なら入れておく。0.11.0。保存には触れない）
│  ├─ guard.js        ログイン画面の検知、書き込んでよい欄かの判定
│  └─ messages.js     日本語の文言（1か所にまとめる）
├─ content/
│  ├─ sites.js        ★貼り込みの欄の指定。**直すのはここだけ**
│  ├─ statsSites.js   ★読み取りの表（どのページを・どう読むか）。**直すのはここだけ**
│  ├─ fill.js         ページの欄を埋める場所（送信はしない）
│  ├─ read.js         唯一、管理画面の数字を読む場所（ページには1文字も書かない）
│  └─ announce.js     ページが開いたことだけを裏方へ知らせる（アイコンの印のため。0.8.0）
└─ test/              vitest。DOM操作そのものは対象外（実機の領分）
```

`common/` と `content/sites.js` は、裏方（background.js）とページの中で動く部分の**両方が同じファイルを読みます**
（写しを作らないため）。ビルド工程はありません——読み込んで、そのまま動きます。

### テスト

```powershell
npm install
npm test
```

しないことを見張るテスト（`test/redLine.test.js`）は、プログラムの中身を調べて「通信のコードが無いこと」「送信のコードが無いこと」
「Cookie に触れていないこと」「文字列からコードを作っていないこと」「HTMLを組み立てて差し込んで
いないこと」「ページへ要素を差し込んでいないこと」「ページへコードを注入していないこと」「ページと窓越しにやり取りしていないこと」
「新しいタブや窓を開いていないこと」「VS Code を呼ぶ場所が1つで、リンクにデータを載せていないこと」
「権限が増えていないこと」を毎回確かめます。裏方（service worker）と、そこから読み込むファイルも調べる対象です。

0.2.0 で読み取りを足したので、「ページから集めない」の見張りも足しました——**コピーするデータを組み立てるのは
`content/read.js` だけ**（0.9.0 から、束で包む `common/stash.js` も）／**ページの文字を読むのは `content/read.js`（数を読む部分）と `content/fill.js`（欄を埋める部分）だけ**／**読んだものの
行き先は、拡張の中の溜まりとクリップボードだけ**（0.9.0。溜めるのは `chrome.storage.local` だけで、触るのは裏方だけ。sync・ページの保存は使わない）／**ページを見張らない**。
ここに3つ目のファイルが増えたら、それは「しないこと」そのものを変えることになります（ついでに足してよいものではありません）。

`test/history.test.js` も同じように、統合小説執筆環境のプログラム一式が隣にあるときだけ、**同じ読者の反応を
統合小説執筆環境の率の計算（`core/readerRates.ts`）にも通して**、この拡張の集計の率（式・値・材料が欠けたときの理由）が
一致することを確かめます（0.10.0。式を写しで持っているので、片方だけが変わった日に気づくため）。

`test/readEnvelope.test.js` は、統合小説執筆環境のプログラム一式（`novel-ai-assistant`）がこのフォルダーの隣にあるときだけ、
**統合小説執筆環境の、データを受け取る本物の部分**（`core/readerStatsEnvelope.ts`）に、このデータを実際に読ませて確かめます。
このデータの形は2つの拡張の間の取り決めなので、確かめ方を写すのではなく、相手に直接読ませています
（統合小説執筆環境のプログラムが無い環境では、そのテストだけ飛ばします）。

調べるファイルは**その場でフォルダーを辿って集めます**（`node_modules`・`test`・`.git` 以外の
すべて）。手で書いた一覧にすると、新しく足したファイルが検査されないまま通ってしまうためです。
**調べる仕組みそのものが働いているか**——新しいファイルを拾うか、禁止の書き方を本当に見つけられるか——も、
架空のプログラムを読ませて同時に確かめています。

---

## 実機で確かめること（実機未確認の一覧）

### 済んだもの（2026-09-22、作者の Chrome の画面を、開発の作業中に読んで確かめた）

- [x] **カクヨムのタイトル欄・本文欄のセレクタ**（`content/sites.js`）
      → `input#episodeTitle-input[name="title"]`／`textarea#episodeBody-input[name="body"]`。
      **推測していた `name` の形は当たっていた**ので、`id` を先に、`name` をあとに残してある
- [x] **カクヨムの話の新規作成画面のURL** → `/my/works/{作品ID}/episodes/new` で合っていた
- [x] **カクヨムの投稿フォームの起点** → `form#episode-editForm`（`formScopes` の先頭に入れた）
- [x] **`content_scripts.matches` がカクヨムの投稿画面を覆えているか** → 覆えていた
      （0.2.0 で、アクセス数の画面 `https://kakuyomu.jp/works/*/accesses` を足した）
- [x] **本文欄が `textarea` か** → textarea だった。改行が消える心配は無い
- [x] **読者の反応のコピー（0.2.0）を、実際に押す** → 作品管理の画面で押せて、統合小説執筆環境の記録へ
      3件（★1,612／今日のPV 1／今月のPV 667）入った。**ただし「読者からの反応」6欄のうち
      ★しか取れず**、残りは表示が省略形（`1.05M`）だったため落ちていた。0.2.1 で、数字に
      マウスを当てたときに出る正確な数（`data-ui-tooltip-label`）から読むよう直してある
- [x] **アクセス数の画面がページ送りか** → **50話ずつ**で、219話の作品は5ページだった。
      表の行はちょうど50行、「次へ」のリンクは `…/accesses?page=2`。0.2.2 で、次のページが
      あるときに「このページの分だけです」と出すようにした（リンクは押さない・開かない）

### 残っているもの

- [ ] **Narou.fun の作品ページで押して、7つの数（総合P・ブクマ・感想数・レビュー・評価P・評価者数・
      週間読者）が入るか確かめる**（0.6.0）。平均評価・評価頻度が**入らない**のが正しい姿。
      ページの形は開発の作業中に読んだHTMLに合わせたが、拡張を入れて押したことはまだ無い
- [ ] **Narou.fun から読んだデータを統合小説執筆環境が取り込めるか**（統合小説執筆環境の作品の記録に、その作品のなろうのNコードが要る）。
      **ほかの方の作品のページで押したときに、統合小説執筆環境が断るか**も確かめる
- [ ] **Narou.fun の日ごとの表を、拡張を入れて押して確かめる**（0.7.0）。表示件数10のときは
      「日ごと 9」と「表示件数を30に」の知らせ、30のときは「日ごと 29」で知らせが出ないのが正しい姿。
      統合小説執筆環境のグラフに「日ごとのブックマークの増減」が出て、減った日が0の線より下に赤で出るか
- [ ] **記録の日時が「最終取得日時」になっているか**（0.7.0）。統合小説執筆環境の履歴の日時と、メモの
      「日時は Narou.fun の最終取得日時」を見る。同じページを2度取り込んで、記録が増えないことも
- [ ] **0.2.1 の直しを押し直して、「読者からの反応」6欄がすべて取れるか確かめる**
      （フォロワー・PV・★・レビュー・応援・コメント。★しか取れなかったのが直っているか）
- [ ] **作品管理の画面を押して、全話ぶん（219件）が1回で入るか確かめる**（0.4.0）。
      **いちばん確かめたいのは件数**——同じページに、話の行（`tr.episode`）を持つ別の表がもう1つあり、
      拾うと438件（全話が二重）になる。統合小説執筆環境の記録は追記なので、入ってしまうと見分けが付かない
- [ ] **作品管理の話ごとの欄が、応援・応援コメント・PV の3つとも入るか**。
      応援コメントの無い話は、**欄ごと入らない**（0にしない）のが正しい姿
- [ ] **アクセス数の画面を押して、話ごとの数と「次へ」の知らせが出るか確かめる**。
      2ページ目も同じように取り込んで、統合小説執筆環境の記録が正しく積み上がるか（0.2.2）
- [ ] **「今日」「今月」のPVが、正しい数として読めるか**。「今週」は読まない作り
- [ ] **各話の更新日が入るか**（0.5.0）。`td.episode-date` の「2022年4月25日15:07 最終更新」を
      日本時間の日時にする。時刻の無い形・前に別の語がある形は**欄ごと入れない**作り
- [ ] **日ごとのPVが何日ぶん入るか**（0.5.0）。グラフの本数を実機で数えていない（30日ほどと見込み）。
      「今日 ◯ PV」と同じ日は1件にまとめる（表示の側を採る）
- [ ] **話数の読み取り**。「第N話」と書かれていない話は、表の順番で代える（それで合っているか）。
      作品管理の題は「第」が無く全角（`１話　転生`）で、**題の先頭にある数字だけ**を話数として読む
- [ ] **アルファポリスの投稿画面のURLと欄のセレクタ**。パスの見当が3通りあり、どれも未確認
- [ ] **アルファポリスの管理画面の形**。読み取りは表に枠だけ置いて止めてある（`supported: false`）
- [ ] **アルファポリスの作品IDの形**。URLで作者IDと作品IDが分かれる形があり、コピーしたデータの作品IDと
      どちらを突き合わせるべきか不明。分かったら `sites.js` の `workIdPatterns`（いまは空）へ書けば照合が戻る
- [ ] **アルファポリスの投稿フォームの起点セレクタ**（`sites.js` の `formScopes`）。欄探しはこの配下に
      限っている。起点が見つからないときはページ全体を探すが、その場合は当たった欄を「見当」扱いにして
      必ず確認を出す。また `manifest.json` の `matches` がその画面を覆えているか（覆えていないと拡張が入らない）
- [ ] **アルファポリスの本文の改行**が正しく入るか（欄が `contenteditable` の作りだった場合、
      改行が消える恐れがある。カクヨムは `textarea` だと確かめた）
- [ ] **画面が入力に気づくか**（React などで作られた画面では、値を入れても気づかれないことがある。
      ネイティブの setter 経由で `input`/`change` を配る対策を入れてあるが、実機で要確認）
- [ ] **0.8.0：アイコンの印**。カクヨムの話の作成画面で **貼**、作品管理・アクセス数と Narou.fun の作品ページで **読**、
      それ以外（作品の公開ページ・話の編集画面など）で印が無いか。タブを切り替えても、そのタブの印のままか
- [ ] **0.8.0：右クリックの項目**。印のある画面だけに「統合小説執筆環境ヘルパー：…」が出て、印の無い画面・ほかのサイトでは出ないか。
      タブを切り替えた直後にも、そのタブに合った項目になっているか
- [ ] **0.8.0：クリップボードの読み書き**が、画面に出ない受け渡しのページ（`offscreen`）で許可なしに通るか
      （`clipboardRead`／`clipboardWrite` で足りるはず。貼り込み・読み取りの両方で）
- [ ] **0.8.0：お知らせ**が画面の隅に出るか（Windows の通知の設定で Chrome がオンのとき）。
      貼り込みの確認ダイアログで30秒以上待ってから決めても、結果のお知らせが出るか
- [ ] **0.8.0：読み取りのあとに VS Code が前に出るか**。Chrome が「Visual Studio Code を開きますか？」と尋ね、
      開いたあとも**読み取った画面がそのまま残っている**こと（タブが別のページへ移ったり、白くなったりしない）。
      統合小説執筆環境の受け側ができるまでは、VS Code が前に出るだけで取り込みは始まらない
- [ ] **0.8.0：入れた直後に説明のページが1回だけ開き**、拡張を再読み込み（更新）したときには開かないか
- [ ] **0.9.0：開いたら溜まるか。** カクヨムの作品管理を開くと、少しして印が「読1」になるか。続けてアクセス数を開くと「読2」、
      「次へ」で2ページ目を開くと「読3」になるか（**2ページ目で印が付くか**が、範囲 `accesses?*` を足した直しの確認）。
      同じページを開き直しても数が増えないか
- [ ] **0.9.0：他人の作品が溜まらないか。** ほかの方の作品の Narou.fun のページ・カクヨムのアクセス数を開いて、
      印が **?** になり、数が増えないこと。押すと「ご自分の作品ですか？」のお知らせが出て、**ボタン（覚える／覚えない）が出るか**
      （Windows の通知でボタンが出ないときは、説明のページの「覚える」で答えられる）
- [ ] **0.9.0：まとめて渡す。** 押すと「まとめて渡しました（N画面・…）」が出て、印が消え、VS Code が前に出るか。
      クリップボードの中身が `"kind":"novelai-stats-bundle"` で始まる束になっているか。統合小説執筆環境の受け取り口が
      できたら、全部が取り込まれるか
- [ ] **0.9.0：説明のページ。** 溜まりの一覧・もう一度渡す・溜まりを空にする・覚えた作品の欄（直して保存・形の合わない行は保存しない）
- [ ] **0.9.0：Chrome を閉じて開き直しても**、溜まりと印の数が残っているか
- [ ] **0.10.0：入れた直後の説明のページ。** 「はじめに」の3つの手順が先頭に出て、統合小説執筆環境を持っていなくても
      迷わず読めるか
- [ ] **0.10.0：記録と集計。** カクヨムの作品管理を開いてから、アイコンを右クリック →「読者の反応の集計を見る」で
      説明のページが開き、その作品の「いまの数」「日ごとの数」「率」が出るか。**表の字が縦に揃っているか**（全角と半角の幅）。
      率の「実際の数」が、統合小説執筆環境の同じ作品の率と同じか
- [ ] **0.10.0：次の日も開いて、記録した日ごとの数が2行になり、増え方（+N）が出るか。** 同じ日に開き直しても行が増えないか
- [ ] **0.10.0：まとめて渡したあとも、集計が消えていないか。** 覚えた作品から外すと、その作品が集計から消えるか
- [ ] **0.10.0：統合小説執筆環境へのリンク。** 押したときだけ新しいタブで Marketplace のページが開くか
- [ ] **0.11.0：更新して入れたとき**（0.10.0 から再読み込み）、説明のページの「統合小説執筆環境へ渡す」が**入っている**か。
      アイコンの「読N」がこれまでどおり出て、押すと渡せるか
- [ ] **0.11.0：切ったとき。** アイコンの「読N」が消え、作品管理を開いたときの印が数字の無い「読」になるか。
      話の作成画面以外で押すと説明のページ（集計）が開き、Chrome が「Visual Studio Code を開きますか？」と尋ねないか。
      話の作成画面では、切っていても「貼」の印が出て、押すと貼り込めるか（0.11.1）。
      作品管理を開き直しても溜まりの数が増えず、集計には記録されるか。入れ直すと、切る前の溜まりが渡せるか
- [ ] **0.11.0：新しく入れたとき**（別の Chrome のプロフィールなどで入れ直す）、「統合小説執筆環境へ渡す」が**切ってある**か
- [ ] 別作品の投稿画面を開いた状態で押し、**取り違えが止まる**こと
- [ ] ログイン画面で押し、**何もしない**こと
